source: slave
target: itsql

defaults:
  object: '{ENV_SCHEMA}_dl.outbound_esvi_{stream_table}_intelix'
  mode: backfill
  primary_key: [id]
  update_key: modified_time
  source_options:
    range: '{START},{END}'

env:
  SLING_LOADED_AT_COLUMN: timestamp

streams:
  dyn_outbound_esvi.tms_prospect:
    sql: |
      WITH source AS (
        SELECT
          *
          ,COALESCE(last_info_time, created_time) AS modified_time
        FROM {stream_schema}.{stream_table}
      )
      SELECT 
        *
      FROM source
      WHERE {incremental_where_cond}
      ORDER BY modified_time
    meta:
      dagster:
        asset_key: 'outbound_esvi_tms_prospect_intelix'
        deps: 'outbound_esvi_tms_prospect'
        group: 'outbound_esvi'
